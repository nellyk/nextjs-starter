import LoginButton from '../components/LoginButton';

const Home = () => (
  <div className="home">
    <h1>Connect to Graph API</h1>
    <div className="card-grid">
      <LoginButton />
    </div>
  </div>
);

export default Home;
